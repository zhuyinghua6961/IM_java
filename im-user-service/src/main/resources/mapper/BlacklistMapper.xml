<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.im.user.mapper.BlacklistMapper">

    <resultMap id="BaseResultMap" type="com.im.user.entity.Blacklist">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="blocked_user_id" property="blockedUserId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 插入黑名单记录 -->
    <insert id="insert" parameterType="com.im.user.entity.Blacklist" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blacklist (
            user_id,
            blocked_user_id,
            status,
            create_time
        ) VALUES (
            #{userId},
            #{blockedUserId},
            #{status},
            NOW()
        )
    </insert>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT *
        FROM blacklist
        WHERE id = #{id}
    </select>

    <!-- 查询用户的黑名单列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT *
        FROM blacklist
        WHERE user_id = #{userId}
          AND status = 1
        ORDER BY create_time DESC
    </select>

    <!-- 查询特定的黑名单记录 -->
    <select id="selectByUserIdAndBlockedUserId" resultMap="BaseResultMap">
        SELECT *
        FROM blacklist
        WHERE user_id = #{userId}
          AND blocked_user_id = #{blockedUserId}
    </select>

    <!-- 检查是否在黑名单中（双向检查） -->
    <select id="checkBlocked" resultMap="BaseResultMap">
        SELECT *
        FROM blacklist
        WHERE status = 1
          AND ((user_id = #{userId} AND blocked_user_id = #{targetUserId})
               OR (user_id = #{targetUserId} AND blocked_user_id = #{userId}))
        LIMIT 1
    </select>

    <!-- 更新黑名单状态 -->
    <update id="updateStatus">
        UPDATE blacklist
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新黑名单记录 -->
    <update id="updateById" parameterType="com.im.user.entity.Blacklist">
        UPDATE blacklist
        SET status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

</mapper>
