<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.im.ai.mapper.FaqMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.im.ai.entity.Faq">
        <id property="id" column="id"/>
        <result property="question" column="question"/>
        <result property="answer" column="answer"/>
        <result property="category" column="category"/>
        <result property="keywords" column="keywords"/>
        <result property="priority" column="priority"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, question, answer, category, keywords, priority, status, create_time, update_time
    </sql>

    <!-- 查询所有启用的FAQ -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM faq
        WHERE status = 1
        ORDER BY priority DESC
    </select>

    <!-- 根据关键词搜索FAQ -->
    <select id="searchByKeyword" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM faq
        WHERE status = 1
          AND (question LIKE CONCAT('%', #{keyword}, '%')
               OR keywords LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY priority DESC
        LIMIT #{limit}
    </select>

    <!-- 根据分类查询FAQ -->
    <select id="findByCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM faq
        WHERE status = 1
          AND category = #{category}
        ORDER BY priority DESC
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM faq
        WHERE id = #{id}
    </select>

</mapper>
